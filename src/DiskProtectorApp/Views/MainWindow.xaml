<controls:MetroWindow x:Class="DiskProtectorApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        xmlns:local="clr-namespace:DiskProtectorApp.Controls"
        mc:Ignorable="d"
        Title="DiskProtectorApp v2.3.0" 
        Height="500" Width="800"
        WindowStartupLocation="CenterScreen"
        GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
        BorderThickness="1"
        BorderBrush="{DynamicResource MahApps.Brushes.Accent}"
        Background="{DynamicResource MahApps.Brushes.ThemeBackground}"
        Foreground="{DynamicResource MahApps.Brushes.Text}"
        Icon="pack://application:,,,/DiskProtectorApp;component/Resources/app.ico">
    
    <controls:MetroWindow.RightWindowCommands>
        <controls:WindowCommands>
            <Button Click="HelpButton_Click" ToolTip="Ayuda">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="HelpCircle" />
                    <TextBlock Margin="4 0 0 0" VerticalAlignment="Center" Text="Ayuda" />
                </StackPanel>
            </Button>
        </controls:WindowCommands>
    </controls:MetroWindow.RightWindowCommands>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Barra de herramientas -->
        <ToolBarTray Grid.Row="0">
            <ToolBar Style="{StaticResource MahApps.Styles.ToolBar}">
                <Button Command="{Binding RefreshCommand}" 
                        ToolTip="Actualizar lista de discos">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Refresh" VerticalAlignment="Center"/>
                        <TextBlock Margin="5,0,0,0" Text="Actualizar" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <Separator/>
                
                <Button Command="{Binding AdminManageCommand}" 
                        ToolTip="Administrar discos no seleccionables">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="ShieldAccount" VerticalAlignment="Center"/>
                        <TextBlock Margin="5,0,0,0" Text="Administrar" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <Button Command="{Binding ProtectCommand}" 
                        ToolTip="Proteger discos seleccionados">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="ShieldLock" VerticalAlignment="Center"/>
                        <TextBlock Margin="5,0,0,0" Text="Proteger" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <Button Command="{Binding UnprotectCommand}" 
                        ToolTip="Desproteger discos seleccionados">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="ShieldOff" VerticalAlignment="Center"/>
                        <TextBlock Margin="5,0,0,0" Text="Desproteger" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
                
                <Separator/>
                
                <Button Command="{Binding ExitCommand}" 
                        ToolTip="Salir de la aplicación">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="ExitToApp" VerticalAlignment="Center"/>
                        <TextBlock Margin="5,0,0,0" Text="Salir" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </ToolBar>
        </ToolBarTray>

        <!-- Información de discos seleccionados -->
        <Border Grid.Row="1" 
                Background="{DynamicResource MahApps.Brushes.Gray10}" 
                Padding="10,5" 
                BorderBrush="{DynamicResource MahApps.Brushes.Gray8}" 
                BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding StatusMessage}" 
                           VerticalAlignment="Center" 
                           FontWeight="SemiBold"/>
            </StackPanel>
        </Border>

        <!-- Lista de discos -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Disks}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="True"
                  CanUserSortColumns="True"
                  IsReadOnly="False"
                  Margin="10"
                  SelectionMode="Extended"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="{DynamicResource MahApps.Brushes.Gray5}"
                  VerticalGridLinesBrush="{DynamicResource MahApps.Brushes.Gray5}">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow" BasedOn="{StaticResource MahApps.Styles.DataGridRow}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSystemDisk}" Value="True">
                            <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray9}" />
                            <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Gray5}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsSelectable}" Value="False">
                            <Setter Property="Background" Value="{DynamicResource MahApps.Brushes.Gray9}" />
                            <Setter Property="Foreground" Value="{DynamicResource MahApps.Brushes.Gray5}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <!-- Columna de Selección con Binding Corregido -->
                <DataGridTemplateColumn Header="Seleccionar" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                      IsEnabled="{Binding IsSelectable}"
                                      HorizontalAlignment="Center" 
                                      VerticalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTextColumn Header="Unidad" 
                                    Binding="{Binding DriveLetter}" 
                                    Width="80"/>
                <DataGridTextColumn Header="Nombre" 
                                    Binding="{Binding VolumeName}" 
                                    Width="200"/>
                <DataGridTextColumn Header="Tamaño Total" 
                                    Binding="{Binding TotalSize}" 
                                    Width="100"/>
                <DataGridTextColumn Header="Espacio Libre" 
                                    Binding="{Binding FreeSpace}" 
                                    Width="100"/>
                
                <!-- Columna de Estado con Control Personalizado -->
                <DataGridTemplateColumn Header="Estado" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <!-- Usar el control personalizado -->
                                <local:DiskStatusIndicator Disk="{Binding}" 
                                                          Width="12" Height="12" 
                                                          Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding ProtectionStatus}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Barra de estado mejorada -->
        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <StackPanel Orientation="Horizontal">
                    <controls:ProgressRing IsActive="{Binding IsWorking}" 
                                          Width="16" Height="16" 
                                          Margin="0,0,10,0"/>
                    <TextBlock Text="{Binding StatusMessage}" 
                               VerticalAlignment="Center"/>
                </StackPanel>
            </StatusBarItem>
            <!-- Información adicional de discos seleccionados -->
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock Text="{Binding SelectedDisksInfo}" 
                           VerticalAlignment="Center"
                           FontStyle="Italic"
                           FontSize="11"
                           Margin="10,0"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</controls:MetroWindow>
